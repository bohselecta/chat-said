# Public verification trigger

This branch triggers the corrected public-repository materialization workflow for the complete ChatSaid/Taurus system.

Attempt: 2 — unconditional runner startup, source archive assertion inside the checked-out workspace.
